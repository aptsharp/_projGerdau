import { FiltroFornecedorPipe } from './filtro-fornecedor.pipe';

describe('FiltroFornecedorPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroFornecedorPipe();
    expect(pipe).toBeTruthy();
  });
});
